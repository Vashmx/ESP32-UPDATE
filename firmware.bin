import ota_update  # Importamos el módulo de actualización OTA

# Conectarse a Wi-Fi y verificar si hay actualizaciones
ota_update.connect_wifi()
ota_update.check_for_update()

# Código para el parpadeo de 5 pulsos por segundo con un descanso de 2 segundos
from machine import Pin
import time

led = Pin(2, Pin.OUT)  # Cambiar el número del pin si es necesario

while True:
    # Realiza 5 parpadeos rápidos
    for _ in range(5):
        led.on()            # Enciende el LED
        time.sleep(0.1)     # Mantén el LED encendido por 100 ms
        led.off()           # Apaga el LED
        time.sleep(0.1)     # Mantén el LED apagado por 100 ms
    
    # Descansa 2 segundos antes de repetir el ciclo
    time.sleep(2)
