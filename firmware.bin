import machine
import time

led = machine.Pin(2, machine.Pin.OUT)

while True:
    led.on()
    time.sleep(0.167)  # Encendido 0.167 segundos
    led.off()
    time.sleep(0.167)  # Apagado 0.167 segundos
